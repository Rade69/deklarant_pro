[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "asycuda-pro"
version = "0.1.0"
description = "ASYCUDA Pro - Aplikacija za carinske deklaracije"
requires-python = ">=3.10"
dependencies = [
    # GUI Framework
    "PySide6>=6.6.0",
    # Database
    "psycopg2-binary>=2.9.7",
    # Configuration Management
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "python-dotenv>=1.0.0",
    # PDF Processing
    "tabula-py>=2.8.0",
    "PyPDF2>=3.0.0",
    "pdfplumber>=0.10.0",
    # Excel Processing
    "openpyxl>=3.1.0",
    # XML Processing
    "lxml>=4.9.0",
    # Logging and Utilities
    "colorama>=0.4.6",
    # Qt Utilities
    "qtawesome>=1.2.0",
    # PDF Export
    "reportlab>=4.0.0",
    # Excel (stari .xls format)
    "xlrd>=2.0.0",
    # AI Agent
    "anthropic>=0.25.0",
    "openai>=1.0.0",
    "mcp>=1.0.0",
    "ollama>=0.1.0",
]

[project.optional-dependencies]
# OCR podrška za skenirane PDF-ove (zahtijeva Tesseract sistemski paket)
# Instalacija: uv sync --extra ocr
ocr = [
    "pytesseract>=0.3.10",
    "pdf2image>=1.16.3",
    "Pillow>=10.0.0",
    "opencv-python>=4.8.0",
    "numpy>=1.24.0",
]
# Dev/test zavisnosti
# Instalacija: uv sync --extra dev
dev = [
    "pytest>=7.4.0",
    "pytest-qt>=4.2.0",
]

[project.scripts]
asycuda-pro = "app:main"

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::PendingDeprecationWarning",
]

[dependency-groups]
dev = [
    "pytest>=7.4.0",
    "pytest-qt>=4.2.0",
]
